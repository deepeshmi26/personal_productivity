{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "eas-build-pre-install": "echo '=== EAS pre-install hook ===' && echo \"Node: $(node --version)\" && echo \"Initial pnpm: $(pnpm --version 2>/dev/null || echo none)\" && npm install -g pnpm@10.26.1 --force && echo \"Final pnpm: $(pnpm --version)\" && echo \"pnpm path: $(which pnpm)\"",
    "expo": "bash scripts/eas-expo.sh"
  },
  "private": true,
  "packageManager": "pnpm@10.26.1",
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
